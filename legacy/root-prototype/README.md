# Archived root prototype

This directory preserves the partial Flutter prototype that previously lived at
the repository root. The production Flutter application now lives at the
repository root; its complete platform folders, `pubspec.yaml`, and `lib/`
tree were promoted from the former `focusflow1/` subdirectory.

The archived files are retained for reference only and are not a runnable
Flutter package.
